import React from 'react';
import { BrowserRouter as Router, Link, Route } from 'react-router-dom';

const Contacts = ({ match }) => {
    
    return (
        <div className="bg_1 ">
            CONTACTS COMES HERE <br />
        </div>
    )
}

export default Contacts